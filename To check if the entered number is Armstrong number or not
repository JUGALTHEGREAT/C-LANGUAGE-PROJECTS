#include<stdio.h>
main()
{
	int n,x=0,r=0,a,c;
	printf("\n Enter no==>");
	scanf("%d",&n);
	c=n;
	while(n>0)
	{
		r=n%10;
		x=(x)+(r*r*r);
		n=n/10;
	}
	
     if(x==c)
     {
     	printf("%d is ARMSTRONG NUMBER",c);
	 }
	 else
	 {
	 	printf("%d is not ARMSTRONG NUMBER",c);
	 }
	
}
